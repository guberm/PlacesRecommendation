# Build output
bin/
obj/

# Rider/VS/VSCode
.idea/
.vs/
*.user
*.suo
.vscode/

# .NET user secrets (runtime only, not for source control)
# Keep appsettings.json in source control (no secrets in it)
# Store real keys in: dotnet user-secrets or environment variables

# SQLite database (runtime data)
*.db
*.db-shm
*.db-wal

# Serilog logs
logs/

# OS
.DS_Store
Thumbs.db

# NuGet restore
*.nupkg
.nuget/

# Claude working files
.claude/
tmpclaude-*

# Deployment scripts (contain credentials — stored in private repo)
deploy/

# Publish output (build artifacts + appsettings with keys)
publish/
